[tool.poetry]
name = "{{ project_name }}"
version = "{{ project_version }}"
description = "{{ project_description }}"
authors = [
{{#loop project_authors ","}}
  "{{this}}",
{{/loop}}
]
readme = "{{ project_readme }}"
repository = "{{ project_git_url }}"
